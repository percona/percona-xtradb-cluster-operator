apiVersion: pxc.percona.com/v1
kind: PerconaXtraDBClusterBackup
metadata:
  name: on-demand-backup-minio-force-path
  finalizers:
    - percona.com/delete-backup
spec:
  pxcCluster: demand-backup
  storageName: minio-force-path
